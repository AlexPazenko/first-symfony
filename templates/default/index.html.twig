{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!!!{% endblock %}

{% set users = [{name: 'John'}, {name: 'Susan'}, {name: 'Robert'}] %}

{% set string = "some string \n from \n database" %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1 class="class">Hello {{ controller_name| upper }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/home/apazenko/sites/Lectorium/symfony/first_symfony/src/Controller/DefaultController.php'|file_link(0) }}">src/Controller/DefaultController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/home/apazenko/sites/Lectorium/symfony/first_symfony/templates/default/index.html.twig'|file_link(0) }}">templates/default/index.html.twig</a></code></li>
    </ul>

   {# <ul>
        {% for user in users %}
            <li>{{ user.name }} - you won {{ random_gift[loop.index0] }}</li>
        {% endfor %}
    </ul>
    {% for message in app.flashes('notice') %}
        <div class="flash-notice">
            {{ message }}
        </div>
    {% endfor %}

    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="flash-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}#}

{#    {% for user in users %}

        <p>{{ user.name }}</p>

    {% endfor %}#}  {# users loop #}

    {#{{ path('home') }} <br>
    {{ url('home') }} <br>
    {{ asset('images/logo.php') }} <br>
    {{ absolute_url(asset('images/small_bird.jpg')) }} <br>#}

    {#<img src="{{ absolute_url(asset('images/small_bird.jpg')) }}" alt="Symfony 4 course">#} {# add image using absolute_url #}

    {#<p>The google tracking code is: {{ ga_code }}</p>#}  {#  we are using global variables #}


    {#{{ app.user }}#}
    {#{{ app.request.get('param')}}#}
    {#{{ app.environment }}#}
   {# {{ dump(app.session) }}#}

{#    {{ render(controller(
        'App\\Controller\\DefaultController::mostPopularPosts',
        { 'number': 2 }
    )) }}#} {# add new conroller method which add new view (most_popular_posts.html.twig) #}

{# {% form_theme form 'form_table_layout.html.twig' %}#}

{# FORM CREATION #}
    {#     {{ form_start(form) }}

    {{ form_label(form.title) }}
    {{ form_widget(form.title) }}

    {% if (form.created_at is defined) %}
    {{ form_label(form.created_at) }}
    {{ form_widget(form.created_at) }}
    {% endif %}
    {{ form_label(form.file) }}
    {{ form_widget(form.file) }}
    {{ form_label(form.agreeTerms) }}
    {{ form_widget(form.agreeTerms) }}
    {{ form_label(form.save) }}
    {{ form_widget(form.save) }}

    {{ form_end(form) }}#}

{#    {{ form(form) }}#}

   {# {{ form(form) }}#}

    {% if app.user %}
        <a href="{{ path('logout')}}">logout</a>
    {% else %}
    <a href="{{ path('login') }}">awesome link</a>
    {% endif %}

{#    {% trans %}
    some.key
    {% endtrans %}#}
    {{ 'some.key'|trans|raw }}

   {# {% if is_granted('ROLE_ADMIN') %}
        <a href="....">Delete something, only admin can do this</a>
    {% endif %}

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <p>Username: {{ app.user.username }}</p>
    {% endif %}#}

</div>
{% endblock %}

{% block javascripts %}

  {{ parent() }}

{% endblock %}
